# Raw Idea

Implement GitHub-style CSS, apply default typography settings (18px font, line height, max content width), ensure responsive layout. This is for a macOS Markdown Viewer app built with SwiftUI and WKWebView that already has markdown-it rendering working.
